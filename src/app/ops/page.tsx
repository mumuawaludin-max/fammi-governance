import { ComingSoonCard } from "@/components/ui/ComingSoonCard";

export default function OpsPage() {
  return (
    <ComingSoonCard
      module="Operasional"
      description="Delivery tracker per sekolah, traffic light, bottleneck log, dan progress tiap produk."
    />
  );
}
